import {Group,BufferGeometry,Float32BufferAttribute,LineBasicMaterial,LineSegments} from 'three';
/** Raw diagnostic outlines only. No surfaces or building meshes. */
export function buildDataDebug(data){const root=new Group();root.name='s1-data-outlines';for(const [category,color,y] of [['roads',0xf4c96c,.08],['footways',0x67c9af,.12],['rails',0xe488bf,.18],['buildings',0x75a8ce,.22]]){const positions=[];for(const item of data[category]){const lines=item.points?[item.points]:[item.polygon.outer,...item.polygon.holes].map(r=>[...r,r[0]]);for(const line of lines)for(let i=1;i<line.length;i++)positions.push(line[i-1][0],y,line[i-1][1],line[i][0],y,line[i][1]);}const geometry=new BufferGeometry();geometry.setAttribute('position',new Float32BufferAttribute(positions,3));const line=new LineSegments(geometry,new LineBasicMaterial({color,depthTest:false}));line.name=`s1-${category}`;root.add(line);}return {root,dispose(){root.removeFromParent();root.traverse(o=>{o.geometry?.dispose();o.material?.dispose();});root.clear();}};}
