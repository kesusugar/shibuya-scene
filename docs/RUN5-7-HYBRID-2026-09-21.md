# RUN 5.7 — Hybrid lower-body locomotion, proof of concept

**Verdict: the hybrid works, and I am still not integrating it — because the two gate criteria
that need the bake (RUN 4 transitions, RUN 5 foot IK in the scene) cannot be confirmed without
it, and the brief puts the bake after the gate.**

What is established: CMU 16_45's legs and Quaternius's upper body combine on one skeleton with
no visible break, and the torso lands in *exactly* the orientation the Quaternius clip puts it
— verified bone by bone, not judged by eye.

Nothing is pushed. `citizen.glb` is untouched. No BVH and no clip is committed.
241/241 tests pass (6 new); typecheck clean. RUN 6, Hit, Enter and Exit are not started.

---

## STEP 0 — why the CMU arms look wrong

Measured on both sides, same frames, same quantities — `scripts/cmu/arm-diagnosis.mjs`.

| | source (CMU skeleton) | retargeted (game character) |
| --- | ---: | ---: |
| elbow, mean | 67° | **65°** |
| elbow, minimum | 48° | **47°** |
| shoulder elevation | 1–38° | **12–49°** |
| hand above chest | −0.191 m | **−0.061 m** |

**The answer is both, and the split matters.**

- **The folded elbow is the subject's own.** 67° against 65° — the retarget reproduces it to
  within 2°. There is nothing to fix; the subject ran that way.
- **The arm *height* is the retarget.** The shoulder sits a consistent **+10°** higher and the
  hand **13 cm** higher relative to the chest, which is a rest-pose difference in the shoulder
  chain (CMU's clavicle is a zero-length helper; the character's is a real bone).

Either way the response is the same and it is what this run does: **do not use the CMU arms.**
The cause is recorded, and per the brief no time was spent fixing it.

*A correction to RUN 5.6, which called the elbow "40°":* that was the minimum across 60 samples
of `validate.mjs`. The mean is 65–67° and the range is 47–82°. A textbook running arm is nearer
90°, so the clip is still more folded than typical — but 40° overstated it.

## STEP 1–4 — the construction

`scripts/cmu/hybrid.mjs`. One skeleton, one clip, composed **offline**. There is no runtime
blending, no second mixer and no second skinned mesh — the game would load the result as an
ordinary Run.

| | bones |
| --- | --- |
| from CMU | `pelvis`, `thigh/calf/foot/ball` × 2 |
| from Quaternius | `spine_01..03`, `neck_01`, `Head`, `clavicle/upperarm/lowerarm/hand` × 2 |

**Shared phase (STEP 3).** Both sources are sampled at the same normalized gait phase with each
clip's own left-foot contact as phase zero — Quaternius's from `gaitDetail.leftContact`, CMU's
from the cycle extraction (16_45 leads with the *right* foot, so its cycle is rolled by 0.51).
Legs and arms are never on separate time axes.

**Root (STEP 4).** Vertical only, from CMU, in the pelvis's parent frame. Horizontal travel is
discarded — the controller remains the source of truth — and survives only as `stride` metadata
for RUN 4's `period = stride / speed`. A test asserts the clip cannot translate.

### STEP 5 — the hips→spine boundary, and the one quaternion that fixes it

The first hybrid **visibly hunched**. A bone's local rotation is relative to its parent, so a
Quaternius spine authored against a Quaternius pelvis, grafted onto a CMU pelvis that is
already pitched forward, compounds the two tilts.

The fix is one quaternion at the boundary bone, applied once at bake time:

```
spine_01_local  ←  (CMU pelvis)⁻¹ · (Quaternius pelvis) · spine_01_local
```

The torso then lands in the world orientation Quaternius intended while everything below stays
CMU's, and every descendant inherits the correction for free. No per-frame solver.

**It works exactly.** Mean tilt from world-up, per bone:

| variant | pelvis | spine_01 | spine_03 | neck_01 | Head |
| --- | ---: | ---: | ---: | ---: | ---: |
| A current Quaternius | 20° | 13° | 42° | 36° | 23° |
| B full CMU | 23° | 22° | 7° | 20° | 9° |
| C hybrid, no compensation | 23° | 16° | 46° | 40° | 26° |
| **E hybrid + boundary fix** | 23° | **13°** | **42°** | **36°** | **23°** |

E reproduces A's upper body to the degree on every bone.

## STEP 6–7 — metrics

`node scripts/cmu/compare-variants.mjs`, all at 4.2 m/s, no foot IK, one character.

| variant | step | step/ref | cadence | penetration | elbow | arm swing |
| --- | ---: | ---: | ---: | ---: | ---: | ---: |
| A current Quaternius | 2.38 m | 1.56 | 106 | 18 mm | 92° | 722 mm |
| B full CMU 16_45 | 1.34 m | 0.88 | 188 | 0 | 66° | 251 mm |
| C hybrid (legs+pelvis) | 1.34 m | 0.88 | 188 | 0 | 92° | 708 mm |
| D hybrid (legs only) | 1.34 m | 0.88 | 188 | **158 mm** | 92° | 683 mm |
| **E hybrid + boundary fix** | **1.34 m** | **0.88** | **188** | **0** | **92°** | **684 mm** |

- **Lower body: E keeps all of CMU's gain.** step/ref 1.56 → 0.88, which is 4.6× closer to the
  1.48–1.58 m the speed and a 160–170 spm cadence imply. Penetration 18 mm → 0.
- **Upper body: E recovers all of Quaternius's.** Elbow 92° (identical to A), arm swing 684 mm
  against A's 722 and full CMU's 251.
- **D is out.** Taking the Quaternius pelvis while using CMU legs breaks the vertical
  relationship and the feet punch **158 mm** through the road.

**The legs are provably untouched**: comparing E's lower-body tracks against the full CMU clip
at 24 phases across 9 bones, the worst difference is **0.022°**. That is asserted by a test, so
it cannot drift silently.

### A metric that is not fit to separate these variants

`drift` reads 100 / 174 / 237 / 125 / 237 mm for A–E. **Ignore the differences between B, C and
E**: their lower-body tracks are identical to a hundredth of a degree, so their true drift is
identical, and the spread of ~60 mm comes from 21 keys and 25 keys landing differently on the
sampling grid at 188 spm. Three separate explanations were tried before the tracks were
compared directly — key snapping (fixed, barely moved it), loop-seam wrap (fixed, barely moved
it), threshold placement (fixed, barely moved it). The measurement is quoted here as a band,
not a figure, and the limitation is recorded in the script.

## STEP 8 — visual comparison

`evidence/run5-7-hybrid/`, all at 4.2 m/s, same camera, same ground, same character:
`three-way-{side,rear,front,feet,upper-body}.png`, `current-vs-hybrid-side.png`,
`boundary-fix-C-vs-E.png`.

- **Legs** — the current row splits into a bound and hangs there; E has a compact stride with
  real knee drive and feet that come down and roll.
- **Upper body** (`three-way-upper-body.png`) — E's arms swing like A's; full CMU's are held
  compactly near the chest. The difference is obvious at this crop and nearly invisible at
  full-figure size, which is why the close-up exists.
- **A caution about reading these sheets.** In the three-row version I first read E's torso as
  hunched. It is not — it matches A to the degree, as the table above shows. Comparing rows
  whose legs and framing differ misleads; the two-row `current-vs-hybrid-side.png` and the
  numbers are the honest comparison.

## STEP 9–11 — what is and is not verified

| | |
| --- | --- |
| **Verified** | bone mask has no duplicate ownership; no world translation; gait metadata valid; unit quaternions; no NaN; loop closes; lower body identical to the CMU source. Six tests, in CI. |
| **Verified** | no second mixer, no second skeleton, no second skinned mesh. The composition is offline and the output is one ordinary clip. |
| **Not verified** | RUN 4 transitions (idle→walk→run, turns, stop) and RUN 5 foot IK on kerb and ramp **in the benches**. Both need the clip inside `citizen.glb`, which is the STEP 10 "if hybrid wins" bake. |
| **Not verified** | CPU, payload and allocation deltas, for the same reason — there is nothing in the runtime yet to measure. |

## STEP 12 — decision

| criterion | result |
| --- | --- |
| 1. legs clearly better than current | **yes** — 4.6× closer to the reference step |
| 2. upper body clearly better than full CMU | **yes** — elbow 92° vs 66°, swing 684 vs 251 mm |
| 3. does not lose Quaternius's arm quality | **yes** — matches A to the degree |
| 4. no visible break at hip/spine | **yes**, after the boundary fix; C without it does break |
| 5. RUN 4 transitions maintained | **not established** — needs the bake |
| 6. RUN 5 foot IK maintained | **not established** — needs the bake |
| 7. performance regression slight | **not established** — nothing in the runtime yet |
| 8. beats current overall on sight | **yes** on the sheets available |

**Selected: HYBRID E, conditionally — and not integrated in this run.**

Five of the eight criteria are met on evidence and three cannot be answered without doing the
thing the gate is supposed to authorise. I am not going to call a gate passed on criteria I
have not tested, and I am not going to bake a clip into the character asset when the brief
says this run is a POC. Those three are a short, well-defined next step, not an open question.

The honest summary: **the CMU route works.** RUN 5.6 found the legs and lost the arms; RUN 5.7
gets both, and the thing that made it work is one quaternion at the hips, not a solver.

## Known limitations

- **The loop seam is real.** 16_45's cycle has 90 mm of pose discontinuity at the cut — the best
  of the CMU pool, but not zero, and it has not been hidden with a crossfade.
- **Cadence overshoots.** 188 spm against a 160–170 reference. Better than the current 106, but
  the step is 0.88 of reference rather than 1.00.
- **One subject, one trial.** 16_45 is subject 16's build and running style. Only 17 of ~2,600
  trials were examined.
- **The CMU shoulder rest offset is diagnosed, not fixed.** It does not matter while the arms
  are unused, and it would matter immediately if anyone tried to use a CMU upper body.
- **`drift` cannot separate these variants**, as recorded above.

## Licence and provenance

Unchanged from RUN 5.6: CMU places no restrictions, the cgspeed conversion adds none, free for
commercial use worldwide. `evidence/run5-6-cmu/CMU-READMEFIRST.txt` holds the verified text.
No new asset was introduced. No raw BVH and no derived clip is committed; the POC JSON lives in
a gitignored `qa/gta-upgrade/cmu-clips/`. If the hybrid is ever adopted, the derived clip and
the NSF/`mocap.cs.cmu.edu` acknowledgment go into `docs/CHARACTER-ASSET-PROVENANCE.md` under
the existing rules.

---

# PHASE A — Hybrid E integration gate

**Adopted.** The hybrid is now the character's `Run` clip, baked into `citizen.glb` as an
ordinary AnimationClip. Nothing at runtime samples two sources or knows there were ever two.

## A1–A2 — the bake

`assets/character/hybrid-run.json` (17.8 KiB) is the one CMU-derived artefact in the repository:
one gait cycle of quaternions on the game's own skeleton, with provenance for each half beside
it. `scripts/convert-character.mjs` folds it into `citizen.glb` in place of the upstream
`Jog_Fwd_Loop`. If the file is absent the build falls back to the upstream Run without comment,
so a checkout without it still produces a working character.

Late loading, the baked fallback figure, the 65-bone skeleton and the `CharacterAsset`
abstraction are all untouched. The original clip is recoverable by deleting one file and
re-running the converter.

## A3 — RUN 4, and two things that had to be fixed

Integration surfaced two genuine faults that the POC could not have shown, because both live in
the blend rather than in the clip.

**The cadence clamp was silently reintroducing foot sliding.** `LOCOMOTION.minPeriod` was
0.72 s — 167 spm — which was ample for a Run authored at 129 spm and became the binding
constraint the moment a 169 spm Run arrived. At 4.2 m/s the blend wanted a 0.640 s cycle, got
0.720, and the feet implied **3.73 m/s while the body moved at 4.2**. That is exactly the
sliding RUN 4 was built to remove, coming back through the one line in the file documented as
the only place it can. Raised to 0.62 s (194 spm), which puts the fastest clip at 1.14×.

**Sprint came back into the gameplay blend.** RUN 4 removed it because its contacts are 0.692
of a cycle apart rather than 0.5, so blending it against a symmetric clip limps — but that
removal held only *arithmetically*, because the Run of the day was authored at 5.36 m/s and
4.2 fell short of it. A Run authored at 3.79 m/s makes 4.2 fall *past* it, and the ladder
bracketed upward into Sprint at 9 % weight.

The fix states the intent instead of relying on the arithmetic: a clip whose contacts are not
near half a cycle apart is kept out of the gameplay blend, and the ladder stops at the first
rung covering the top gameplay speed. Sprint stays in the full ladder and is still reachable
above the gameplay range. Two speed-only filters were tried first and each broke one of the two
configurations — `speed <= top` throws away a 5.36 m/s Run and leaves a walk at 2.15×, and
"first rung reaching top" lets Sprint in whenever the Run falls short. Symmetry decides both
correctly because it names the actual defect.

With both fixed, the blend implies exactly the body's speed at every point in the range:

| speed | before (Quaternius Run) | after (hybrid Run) |
| --- | --- | --- |
| 1.5 | Walk 0.88 @1.15× · Run 0.12 @0.80× | Walk 0.81 @1.28× · Run 0.19 @0.68× |
| 2.5 | Walk 0.65 @1.29× · Run 0.35 @0.90× | Walk 0.46 @1.63× · Run 0.54 @0.86× |
| 3.4 | Walk 0.45 @1.35× · Run 0.55 @0.95× | Walk 0.14 @1.82× · Run 0.86 @0.97× |
| 4.2 | Walk 0.26 @1.39× · Run 0.74 @0.97× | **Run 1.00 @1.11×** |
| 7.0 | Run 0.43 @1.24× · Sprint 0.57 @0.88× | Run 0.28 @1.05× · Sprint 0.72 @0.99× |

**One honest regression.** The partner clip now stretches further at mid speeds — Walk reaches
1.82× at 3.4 m/s, against 1.35× before — because the hybrid Run is 169 spm against Walk's 90,
where the old pair were 1.43× apart. Rendered at 2.5 m/s (`mid-band-2.5ms.png`) it reads as a
jog, not as a fast-forwarded walk, with the sole 1 mm from the road on average; at 3.4 m/s Walk
is down to 14 % weight. The rate test was reformulated around the **dominant** clip, which is
what the defect it was written for actually was (a Sprint at 0.51× that was most of what was on
screen). The partner bound is looser and stated as such. That is a relaxation and it is
recorded here rather than buried.

Transitions themselves are clean: `transitions-flat.png` shows 1.50 Walk → 2.62 Run → 4.20 Run
with no pop.

## A4 — RUN 5, unchanged and better

The solver was not touched. Every number improved because the input got better:

| scene | before | after |
| --- | ---: | ---: |
| kerb, astride | 72.3 → 10.7 mm | 72.3 → **10.7 mm** (unchanged; a standing test) |
| kerb ramp, walking | 13.7 → 12.1 mm | 13.7 → **6.1 mm** |
| flat road, **running** | 49.0 → 49.0 mm | 49.0 → **38.2 mm** |
| flat pavement | 0 solves | **0 solves** |

Flat ground still stands the solver down completely — ~1300 ground queries and **zero**
leg-solves per scenario — and the kerb ground still solves only where the path crosses it
(10–14 per scenario). The 22 % improvement in running contact came from the clip, not from
strengthening the IK, which is the boundary RUN 5 drew.

## A5 — payload

| | raw | gzip |
| --- | ---: | ---: |
| before | 1,443,376 | 639,504 |
| after | **1,414,612** | **631,129** |
| delta | **−28,764** | **−8,375** |

Smaller, because the hybrid is 25 keys where the upstream Jog was denser. The 17.8 KiB derived
clip is a build input and is not shipped. Draw calls and mixer actions are unchanged — it is
one clip replacing one clip.

## A7 — decision

| criterion | result |
| --- | --- |
| 1. RUN 4 transitions not broken | met, after fixing the clamp and the Sprint bracket; one documented regression in the partner clip's playback rate |
| 2. RUN 5 IK not broken | met, and improved on ramp and road without touching the solver |
| 3. performance regression slight | met — payload smaller, draw calls unchanged |
| 4. visual quality clearly higher | met — step/ref 1.56 → 0.88 |
| 5. upper body keeps Quaternius quality | met — identical, by construction and by measurement |
| 6. lower body keeps CMU quality | met — 0.022° from the source |

**Adopted.** 243/243 tests (3 new), typecheck clean.
